# Rules

- Only run `pnpm build`, `pnpm lint`, or `pnpm test` when explicitly requested by the user.
- Follow JavaScript Standard Style (standard.js) when writing `.js` and `.jsx` files.

- 禁止运行 computer use, browser use 用于验收，用户会截图给你